package com.example.ordinariluisandroid.UI.fragment
/*
import androidx.fragment.app.Fragment
import com.example.ordinariluisandroid.databinding.FragmentDetalleBinding
import com.example.ordinariluisandroid.model.Launch

import com.example.ordinariluisandroid.databinding.FragmentLanzamientosBinding
import com.google.firebase.database.FirebaseDatabase
    private var launch: Launch? = null

   private lateinit var database: FirebaseDatabase
   override fun onCreateView(
       inflater: LayoutInflater,
       container: ViewGroup?,
       savedInstanceState: Bundle?
   ): View? {
       binding = FRAGMENT DE DETALLES .inflate(inflater, container, false)
       return binding.root
   }
        return binding.root
    }


    override fun onResume(context: Context) {
       super.onAttach(context)
       database =
         AQUI METEMOS LA LOGICA DEL BOTON QUE METERIAMOS EN LA CARTA PARA PODER VER EL DETALLE
         PRIMERO ESCUCHADNO AL BOTON Y LUEGO
         LLAMAMOS A LA BASE DE DATOS PARA OBTENER LOS DETALLES DEL LANZAMIENTO SELECCIONADO Y MOSTRARLOS EN LA VISTA DE DETALLES
            val tagsText = if (post.tags != null && post.tags.isNotEmpty()) {
                "Tags: " + post.tags.joinToString(", ")
            } else {
                "Sin tags"
            }
            binding.tvTags.text = tagsText
        }
    }
}






























}  */